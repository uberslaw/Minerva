# CpuThrottle.Tray is added only when building on Windows (see README).
# On Linux CI, build with: dotnet build CpuThrottle.sln
# On Windows, also: dotnet sln add src/CpuThrottle.Tray/CpuThrottle.Tray.csproj
